# Typography

<!-- TODO: 13 semantic type styles (display, heading 2xl–xs, body lg–sm, label lg–sm)
     + primitive families/sizes/weights. Values from build/json/tokens.json.
     Note: each semantic style bundles ~7 props (font-family, size, line-height, etc.). -->
