# Tech Writing Reference Files — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create 5 `.md` reference files in `docs/writing-guides/` distilling the Google Tech Writing course series into dense, Claude-readable notes for use when writing Harbor Design System documentation.

**Architecture:** Each file maps 1:1 to one course URL. Content is extracted from the fetched source pages and structured into four sections: key principles, patterns to apply, patterns to avoid, and DS-specific application. No code, no tests — pure content creation.

**Tech Stack:** Markdown, WebFetch for source content, Write tool for file creation.

---

### Task 1: Write `tech-writing-overview.md`

**Files:**
- Create: `docs/writing-guides/tech-writing-overview.md`

- [ ] **Step 1: Create the file**

Write `docs/writing-guides/tech-writing-overview.md` with this exact content:

```markdown
# Tech Writing — Overview

Source: https://developers.google.com/tech-writing/overview

## Co obejmuje kurs

Google oferuje 4 moduły:
- **Technical Writing One** — podstawy (słowa, zdania, listy, akapity, dokumenty)
- **Technical Writing Two** — poziom średni (edycja, duże dokumenty, ilustracje, kod przykładowy, LLM)
- **Tech Writing for Accessibility** — dokumentacja dostępna dla osób z niepełnosprawnościami
- **Writing Helpful Error Messages** — pisanie komunikatów o błędach

## Jak używać tych plików

Przed pisaniem dokumentacji DS, przeczytaj odpowiedni plik z tego katalogu:
- Dokumentacja komponentu / tokenu → `tech-writing-one.md` + `tech-writing-two.md`
- Komunikaty o błędach w komponentach → `tech-writing-error-messages.md`
- Opisy obrazków, diagramów → `tech-writing-accessibility.md`
- Każda dokumentacja → `tech-writing-accessibility.md` (sekcja języka inkluzywnego)
```

- [ ] **Step 2: Verify the file was created correctly**

Read `docs/writing-guides/tech-writing-overview.md` and confirm content is complete.

- [ ] **Step 3: Commit**

```bash
git add docs/writing-guides/tech-writing-overview.md
git commit -m "docs: add tech-writing-overview reference"
```

---

### Task 2: Write `tech-writing-one.md`

**Files:**
- Create: `docs/writing-guides/tech-writing-one.md`

- [ ] **Step 1: Create the file**

Write `docs/writing-guides/tech-writing-one.md` with this exact content:

```markdown
# Tech Writing One — Reference

Source: https://developers.google.com/tech-writing/one

## Kluczowe zasady

### Słowa i terminologia
- Zdefiniuj nowe terminy przy pierwszym użyciu lub linkuj do istniejącej definicji
- Używaj tego samego terminu konsekwentnie w całym dokumencie — nie synonimizuj
- Akronimy: przy pierwszym użyciu napisz pełną formę + akronim w nawiasie, pogrubiając oba: **Protocol Buffers** (**PB**)
- Definiuj akronim tylko jeśli: (a) jest znacznie krótszy niż pełna forma AND (b) pojawia się wielokrotnie
- Zaimki (it, they, this, that) — umieszczaj max. 5 słów od rzeczownika, do którego się odnoszą; przy wątpliwości powtórz rzeczownik zamiast używać zaimka

### Aktywny vs. pasywny głos
- Schemat aktywnego: **podmiot + czasownik + obiekt** ("The compiler generates an error")
- Schemat pasywnego: obiekt + forma "be" + imiesłów ("An error is generated by the compiler")
- Aktywny głos: mniej słów, jasne kto wykonuje akcję, łatwiejszy do czytania
- Pasywny można stosować wyjątkowo gdy wykonawca akcji jest nieistotny lub nieznany
- Zdania rozkazujące ("Open the file", "Click Save") są aktywne — domyślny podmiot to "you"

### Mocne czasowniki
- Unikaj generycznych: is/are/was, occur, happen
- Zastąp mocnym, precyzyjnym czasownikiem:
  - "An error occurs" → "The system raises an error"
  - "There is a bug" → "The component contains a bug"
  - "We are careful" → "We carefully validate"
- Unikaj "There is / There are" — przesuń właściwy podmiot na początek zdania

### Krótkie, jasne zdania
- Jedno zdanie = jedna myśl (jak jedna instrukcja w kodzie)
- Zastąp rozwlekłe frazy zwięzłymi:
  - "at this point in time" → "now"
  - "is able to" → "can"
  - "provides a detailed description of" → "describes"
  - "causes the triggering of" → "triggers"
- Zdania podrzędne: trzymaj je tylko gdy rozwijają główną myśl; jeśli zmieniają temat → nowe zdanie
- **That** (bez przecinka) = klauzula niezbędna: "calculations that avoid linear algebra"
- **Which** (z przecinkiem) = klauzula dodatkowa: "Python, which Guido invented, ..."
- Unikaj przymiotników i przysłówków subiektywnych; zastąp danymi liczbowymi:
  - "runs extremely fast" → "runs 225% faster"

### Listy i tabele
- **Lista punktowana** = elementy bez określonej kolejności; zmiana kolejności nie zmienia sensu
- **Lista numerowana** = kroki lub sekwencje; zmiana kolejności zmienia sens
- **Równoległość** — wszystkie elementy listy muszą mieć tę samą strukturę gramatyczną (np. wszystkie zaczynają się od czasownika)
- Listy numerowane: zacznij każdy element od trybu rozkazującego ("Open", "Configure", "Run")
- Każda lista i tabela wymaga zdania wprowadzającego zakończonego dwukropkiem:
  - "The component accepts the following props:"
- Tabele: krótkie komórki (max. 2 zdania), spójne typy danych w kolumnach, nagłówki kolumn

### Akapity
- Pierwsze zdanie akapitu = najważniejsze; musi wskazywać temat akapitu
- Jeden akapit = jeden temat; usuń zdania odbiegające od tematu
- Długość: 3–5 zdań (optymalnie); unikaj "ścian tekstu" (>7 zdań)
- Framework akapitu: **Co** (informacja) → **Dlaczego** (znaczenie) → **Jak** (zastosowanie)

### Odbiorcy
- Formuła: `dokumentacja = (wiedza potrzebna odbiorcy) − (wiedza, którą już ma)`
- Zidentyfikuj role, doświadczenie, znajomość projektu
- Unikaj "klątwy wiedzy" — nie zakładaj, że odbiorca wie co wiesz Ty
- Używaj prostego języka (wielu czytelników używa tłumacza); unikaj idiomów i kulturowych metafor
- Unikaj sportowych lub kulturowych odniesień (baseball, cricket) — nie są zrozumiałe globalnie

### Struktura dokumentu
- Zacznij od zdefiniowania zakresu: co dokument obejmuje i czego NIE obejmuje
- Podaj wymagania wstępne (co czytelnik musi wiedzieć/zrobić przed czytaniem)
- Kluczowe informacje umieszczaj na początku — zajęty czytelnik może przeczytać tylko pierwszą sekcję
- Łącz nowe koncepcje ze znajomymi: "Podobnie jak X, ale z Y"

### Interpunkcja
- Przecinek Oxford (serialowy): "C++, Python, Java, and JavaScript" — używaj zawsze
- Średnik: łączy dwa silnie powiązane, kompletne zdania
- Myślnik (—): mocniejsza pauza niż przecinek; do wyodrębnienia dygresji
- Dwukropek: przed listą lub tabelą
- Nawiasy: dla drugorzędnych informacji; minimalizuj ich użycie

## Wzorce do stosowania (before → after)

| Przed | Po |
|---|---|
| "Protocol Buffers...later, protobufs..." | "**Protocol Buffers** (**protobufs**) are..." |
| "This is a variable." | "This `counter` variable stores..." |
| "An error is generated." | "The system generates an error." |
| "There is a prop called `size`." | "The `size` prop controls..." |
| "The component runs screamingly fast." | "The component renders in under 16ms." |
| "at this point in time" | "now" |
| "is able to" | "can" |

## Wzorce do unikania

- Synonimizowanie terminów ("Button", "control", "element" dla tego samego komponentu)
- Niezdefiniowane akronimy przy pierwszym użyciu
- Zaimki bez jasnego odniesienia ("It renders the..." — co renderuje?)
- Pasywny głos gdy wiadomo kto jest aktorem
- "There is / There are" na początku zdania
- Subiektywne przymiotniki: "easy", "simple", "powerful", "fast" bez danych

## Zastosowanie w DS

- **Nazwy komponentów**: wybierz jedną nazwę i używaj jej konsekwentnie (np. "Button", nie "Button/button/Btn")
- **Props**: definiuj przy pierwszym wprowadzeniu; używaj kodu inline (`size`, `variant`)
- **Opisy props**: jeden prop = jedno zdanie; zacznij od czasownika w 3. osobie ("Controls the...", "Specifies the...", "Enables...")
- **Listy wariantów**: lista punktowana z równoległą strukturą
- **Kroki użycia**: lista numerowana, każdy krok zaczyna się od trybu rozkazującego
- **Przykłady**: "Użyj `Button` gdy..." zamiast "Można użyć `Button` gdy..."
```

- [ ] **Step 2: Verify the file was created correctly**

Read `docs/writing-guides/tech-writing-one.md` and confirm all 7 sections are present and content is complete.

- [ ] **Step 3: Commit**

```bash
git add docs/writing-guides/tech-writing-one.md
git commit -m "docs: add tech-writing-one reference"
```

---

### Task 3: Write `tech-writing-two.md`

**Files:**
- Create: `docs/writing-guides/tech-writing-two.md`

- [ ] **Step 1: Create the file**

Write `docs/writing-guides/tech-writing-two.md` with this exact content:

```markdown
# Tech Writing Two — Reference

Source: https://developers.google.com/tech-writing/two

## Kluczowe zasady

### Samoedycja
- Przyjmij i przestrzegaj jednego przewodnika stylu (np. Google Developer Documentation Style Guide)
- Pisz w drugiej osobie: zwracaj się do czytelnika "you", nie "we" lub "one"
- Warunki przed instrukcjami: "If you want X, do Y" (nie: "Do Y if you want X")
- Przed oddaniem dokumentu: odczekaj, wróć ze świeżym spojrzeniem, przeczytaj na głos
- Zmiana formatu (druk, zmiana czcionki) pomaga wychwycić błędy niewidoczne na ekranie
- Peer review: recenzent nie musi znać tematu, wystarczy znajomość przewodnika stylu

### Duże dokumenty
- Krótkie dokumenty sprawdzają się lepiej dla nowych odbiorców (how-to, overview, conceptual guide)
- Długie dokumenty sprawdzają się dla doświadczonych (tutorials, best practices, reference)
- Wprowadzenie dokumentu musi zawierać: (1) co dokument obejmuje, (2) wymagana wiedza wstępna, (3) czego dokument NIE obejmuje
- Zweryfikuj pod koniec pisania czy treść odpowiada temu co obiecuje wprowadzenie
- Nagłówki: opisuj aktywność czytelnika ("Configure the token pipeline") nie terminologię ("Token pipeline configuration")
- Pod każdym nagłówkiem umieszczaj krótkie zdanie kontekstu zanim pojawi się podnagłówek
- Treść: stopniowe ujawnianie — od prostego do złożonego; przeplataj koncepcje z ćwiczeniami

### Ilustracje i diagramy
- Tylko *instruktywne* grafiki pomagają w nauce; dekoracyjne grafiki nie
- Podpis pod diagramem: krótki (kilka słów), wyjaśnia co czytelnik powinien z niego wynieść
- Max. jedna porcja informacji (≈jeden akapit lub ≤5 bullet points) na jeden diagram
- Złożone systemy: dziel na podsystemy, osobny diagram na każdy
- Wskazówki wizualne (kółka, strzałki, callouts) kierują uwagę lepiej niż długi tekst
- Kolory: zapewnij wystarczający kontrast; nie przekazuj informacji wyłącznie przez kolor
- Format eksportu: SVG (skalowalny, dobra jakość)
- Pytania rewizyjne: Jak uprościć? Czy podzielić? Czy tekst jest czytelny na tle?

### Kod przykładowy
- Dobry przykładowy kod jest: **poprawny, zwięzły, zrozumiały, reużywalny, sekwencjonowany złożonością**
- Poprawność: musi się budować bez błędów, wykonywać to co deklaruje, być gotowy produkcyjnie (bez podatności)
- Komentarze: wyjaśniaj *dlaczego*, nie *co*; pomijaj oczywiste; dla zaawansowanych wyjaśniaj nieoczekiwane decyzje projektowe
- Identyfikatory: opisowe nazwy klas, metod, zmiennych; bez kryptycznych skrótów
- Sekwencja: zacznij od najprostszego przykładu → średni → zaawansowany
- Każdy przykład kodu: podaj instrukcje setup, zależności, oczekiwany output
- Testy jednostkowe ≠ przykładowy kod: testy weryfikują działanie, przykłady edukują

### Użycie LLM w pisaniu dokumentacji
- Nadaj LLM rolę: "Jesteś doświadczonym technical writerem piszącym dokumentację design systemu"
- Określ odbiorcę, typ dokumentu, cel i styl przed generowaniem
- Ogranicz zakres: "Używaj tylko informacji z poniższego tekstu:"
- Iteruj: ulepszaj prompt stopniowo; gdy edytowanie odpowiedzi jest szybsze niż ulepszanie promptu — edytuj
- Zawsze weryfikuj wygenerowaną treść pod kątem błędów i niespójności
- LLM przydatny do: first draft, reorganizacja, copy-editing, konwersja formatu, streszczenia

## Wzorce do stosowania

| Przed | Po |
|---|---|
| "We recommend configuring..." | "Configure the token..." |
| "Click Submit if you want to save." | "To save, click Submit." |
| Nagłówek: "Token Pipeline" | Nagłówek: "Configure the token pipeline" |
| Jeden wielki diagram całego systemu | Osobne diagramy: tokens → components → themes |

## Wzorce do unikania

- Zwracanie się do czytelnika przez "we" lub "one"
- Warunek po instrukcji: "Do X if you want Y"
- Podnagłówek bezpośrednio pod nagłówkiem bez zdania kontekstu
- Przykładowy kod który się nie kompiluje lub nie robi tego co mówi
- Komentarze wyjaśniające oczywiste rzeczy ("// set x to 5")
- Dekoracyjne grafiki bez wartości informacyjnej

## Zastosowanie w DS

- **Struktura strony komponentu**: wprowadzenie (co to jest, kiedy używać, czego NIE robi) → props → warianty → kod przykładowy (podstawowy → z opcjami → zaawansowany)
- **Kod przykładowy tokenów/komponentów**: importy, setup, oczekiwany output — zawsze kompletny i działający
- **Diagramy relacji tokenów**: osobny diagram per poziom (primitive → semantic → component)
- **Nagłówki sekcji**: opisuj co czytelnik robi ("Use the Button component") nie co to jest ("Button component")
- **LLM do draftu**: podaj istniejący komponent jako kontekst + role "DS technical writer"
```

- [ ] **Step 2: Verify the file was created correctly**

Read `docs/writing-guides/tech-writing-two.md` and confirm all sections are present.

- [ ] **Step 3: Commit**

```bash
git add docs/writing-guides/tech-writing-two.md
git commit -m "docs: add tech-writing-two reference"
```

---

### Task 4: Write `tech-writing-accessibility.md`

**Files:**
- Create: `docs/writing-guides/tech-writing-accessibility.md`

- [ ] **Step 1: Create the file**

Write `docs/writing-guides/tech-writing-accessibility.md` with this exact content:

```markdown
# Tech Writing for Accessibility — Reference

Source: https://developers.google.com/tech-writing/accessibility

## Kluczowe zasady

### Projektowanie dla wszystkich
- Dostępna dokumentacja = czytelna i zrozumiała dla wszystkich, w tym dla osób używających czytników ekranu
- Zasady dostępności stosują się do: design docs, komentarzy w kodzie, UI tekstu, helpów CLI, komunikatów o błędach — nie tylko formalnej dokumentacji
- Sprawdź: czy treść jest zrozumiała bez koloru? bez obrazków? za pomocą samego tekstu?

### Alt text dla obrazków i diagramów
- Każdy obraz informacyjny wymaga alt tekstu
- Alt text opisuje *treść i cel* obrazu, nie jego wygląd ("A diagram showing the token hierarchy from primitive to semantic to component-level" nie "A colorful diagram")
- Dla złożonych diagramów technicznych: alt text + pełny opis tekstowy w treści dokumentu
- Obrazy dekoracyjne: pusty alt (`alt=""`) lub pomiń — czytnik ekranu pominie je
- Długość alt tekstu: tyle ile potrzeba, ale zwięźle; długie opisy → do treści strony

### Kontrast kolorów
- Tekst na tle: min. 4.5:1 (normalna wielkość), 3:1 (duży tekst)
- Nie przekazuj informacji wyłącznie przez kolor (np. "czerwony = błąd" bez ikony lub tekstu)
- Diagramy: wybieraj kolory przyjazne dla daltonistów lub dodawaj wzory/kształty jako drugi kanał informacji

### Język inkluzywny
- Unikaj terminów z historycznym ładunkiem wykluczenia (whitelist/blacklist → allowlist/denylist)
- Unikaj terminów deprecjonujących zdolności ("simple", "easy", "just", "obviously")
- Unikaj kulturowych założeń i idiomów
- Pisz w drugiej osobie ("you") — neutralne płciowo i bezpośrednie
- Przykłady kodu: używaj opisowych, neutralnych kulturowo nazw zmiennych

### Dostępne wizualizacje
- Diagramy: dodawaj tekstowy opis alternatywny w treści (nie tylko alt text)
- Struktura tabeli: zawsze używaj nagłówków kolumn (`<th>`); unikaj tabel do layoutu
- Nie polegaj wyłącznie na pozycji/układzie przestrzennym do przekazania znaczenia
- Zapewnij tekstową wersję treści diagramu (np. jako lista lub tekst opisowy pod diagramem)

### Edycja pod kątem dostępności
- Sprawdź: czy każdy obraz ma alt text?
- Sprawdź: czy informacja przekazana kolorem ma też tekstowy odpowiednik?
- Sprawdź: czy język jest inkluzywny (brak "simple", "easy", "obviously")?
- Sprawdź: czy nagłówki tworzą logiczną hierarchię (H1 → H2 → H3)?
- Sprawdź: czy linki mają opisowy tekst ("See the Button API" nie "Click here")?

## Wzorce do stosowania (before → after)

| Przed | Po |
|---|---|
| "whitelist / blacklist" | "allowlist / denylist" |
| "simple configuration" | "configuration" |
| "just add the token" | "add the token" |
| "obviously, use Button for..." | "use Button for..." |
| Link: "Click here" | Link: "See Button component API" |
| Alt: "image.png" | Alt: "Diagram showing Button variants: primary, secondary, ghost" |

## Wzorce do unikania

- Obrazy bez alt tekstu
- Informacja przekazana tylko przez kolor
- "simple", "easy", "just", "obviously" — zakładają że odbiorca już wie
- "Click here", "Read more" — nieinformacyjne teksty linków
- Tabele używane do layoutu zamiast danych
- Idiomy i metafory kulturowe

## Zastosowanie w DS

- **Każdy diagram tokenów/komponentów**: alt text + tekstowy odpowiednik pod diagramem
- **Przykłady kolorów**: zawsze podawaj wartość hex/token obok próbki koloru
- **Status tokenu (deprecated, experimental)**: oznaczaj tekstem, nie tylko kolorem
- **Linki do Storybook/sandbox**: opisowe teksty ("Open Button in Storybook") nie "here"
- **Sekcja dostępności komponentu**: opisz jak komponent zachowuje się z czytnikiem ekranu, jakie role/atrybuty ARIA eksponuje
- **Unikaj w opisach**: "easy to use", "simply pass", "obviously", "just set"
```

- [ ] **Step 2: Verify the file was created correctly**

Read `docs/writing-guides/tech-writing-accessibility.md` and confirm all sections are present.

- [ ] **Step 3: Commit**

```bash
git add docs/writing-guides/tech-writing-accessibility.md
git commit -m "docs: add tech-writing-accessibility reference"
```

---

### Task 5: Write `tech-writing-error-messages.md`

**Files:**
- Create: `docs/writing-guides/tech-writing-error-messages.md`

- [ ] **Step 1: Create the file**

Write `docs/writing-guides/tech-writing-error-messages.md` with this exact content:

```markdown
# Writing Helpful Error Messages — Reference

Source: https://developers.google.com/tech-writing/error-messages

## Kluczowe zasady

### Fundamentalna zasada
Dobry komunikat o błędzie odpowiada na dwa pytania:
1. **Co poszło nie tak?** (przyczyna)
2. **Jak użytkownik to naprawia?** (rozwiązanie)

Komunikat który nie odpowiada na oba pytania → zły komunikat.

### Wyjaśnianie problemu
- Identyfikuj konkretną przyczynę błędu, nie tylko symptom
  - "Error occurred" ❌ — nie mówi co się stało
  - "The `color` token value `#ZZZ` is not a valid hex color." ✅
- Wskazuj nieprawidłowe wejście użytkownika gdy to ono jest przyczyną
  - Podaj co zostało podane i dlaczego jest nieprawidłowe
- Określaj wymagania i ograniczenia
  - "Token names must start with a letter and contain only letters, numbers, and hyphens."

### Wyjaśnianie rozwiązania
- Powiedz użytkownikowi co konkretnie zrobić
  - "Check the API documentation" ❌
  - "Set `variant` to one of: primary, secondary, ghost, danger." ✅
- Podaj przykład poprawnej wartości gdy to możliwe
- Nie zmuszaj użytkownika do domyślania się rozwiązania

### Pisanie jasno
- **Zwięzłość**: usuń wszystkie słowa które nie wnoszą informacji
  - "An error has occurred in the system" → "The token value is invalid."
- **Brak podwójnych negatywów**: "not invalid" → "valid"; "not disabled" → "enabled"
- **Spójność terminologii**: używaj tych samych nazw co w dokumentacji i UI
- **Format**: dla złożonych błędów używaj list; jedna linia dla prostych błędów
- **Ton**: pomocowy, nie oskarżycielski
  - "You forgot to specify..." ❌
  - "Specify the required `name` prop." ✅
- **Brak żargonu technicznego** (o ile nie piszesz dla deweloperów)

### Złe komunikaty o błędach — wzorce do rozpoznania
- Nieokreślona przyczyna: "Something went wrong", "Invalid value", "Error"
- Brak wskazówki jak naprawić: "Authentication failed" (co zrobić?)
- Nieprecyzyjne informacje: "The value is too long" (jaki limit?)
- Mylący język lub podwójne negatywy
- Nieznane następne kroki: komunikat który nie mówi co dalej

## Wzorce do stosowania (before → after)

| Przed | Po |
|---|---|
| "Invalid token value." | "The token `brand.color.primary` has an invalid value `#ZZZ`. Use a valid hex color (e.g., `#0057FF`)." |
| "Error in configuration." | "The `borderRadius` token is missing. Add it to your `tokens.json` file." |
| "Authentication failed." | "Your API key is invalid or expired. Generate a new key at [link]." |
| "The value is not valid." | "The `spacing` value must be a number in pixels (e.g., `16`). Received: `medium`." |
| "You provided wrong input." | "The `size` prop accepts: `sm`, `md`, `lg`. Received: `large`." |

## Wzorce do unikania

- Generyczne: "Error", "Something went wrong", "Invalid input"
- Oskarżycielskie: "You forgot to...", "You provided..."
- Bez rozwiązania: błąd bez wskazówki co zrobić
- Nieprecyzyjne: brak wartości którą podano, brak oczekiwanej wartości
- Podwójne negatywy: "not an invalid value"
- Żargon nieznany odbiorcy

## Zastosowanie w DS

- **Walidacja tokenów**: "Token `{name}` has value `{value}` which is not a valid {type}. Expected: {format}."
- **Brakujące props**: "The `{component}` component requires the `{prop}` prop. Add `{prop}=\"{example}\"` to your usage."
- **Nieprawidłowe warianty**: "`{value}` is not a valid variant for `{component}`. Use one of: {list}."
- **Błędy TypeScript**: komunikaty w typach powinny wyjaśniać przyczynę i podawać poprawną strukturę
- **Deprecated API**: "The `{prop}` prop is deprecated and will be removed in v{version}. Use `{replacement}` instead."
- **Błędy build-time (np. Style Dictionary)**: podaj plik, linię, token i konkretny problem
```

- [ ] **Step 2: Verify the file was created correctly**

Read `docs/writing-guides/tech-writing-error-messages.md` and confirm all sections are present.

- [ ] **Step 3: Commit**

```bash
git add docs/writing-guides/tech-writing-error-messages.md
git commit -m "docs: add tech-writing-error-messages reference"
```

---

### Task 6: Save memory and final verification

**Files:**
- Modify: `/Users/marcinkwiatkowski/.claude/projects/-Users-marcinkwiatkowski-Desktop-Harbor-Design-System/memory/MEMORY.md`
- Create: `/Users/marcinkwiatkowski/.claude/projects/-Users-marcinkwiatkowski-Desktop-Harbor-Design-System/memory/project_writing_guides.md`

- [ ] **Step 1: Verify all 5 files exist**

```bash
ls docs/writing-guides/
```

Expected output:
```
tech-writing-accessibility.md
tech-writing-error-messages.md
tech-writing-one.md
tech-writing-overview.md
tech-writing-two.md
```

- [ ] **Step 2: Create memory file for the writing guides**

Write `/Users/marcinkwiatkowski/.claude/projects/-Users-marcinkwiatkowski-Desktop-Harbor-Design-System/memory/project_writing_guides.md`:

```markdown
---
name: project-writing-guides
description: Tech writing reference files exist in docs/writing-guides/ — consult before writing any DS documentation
metadata:
  type: project
---

Tech writing reference files live at `docs/writing-guides/` in the Harbor Design System repo.

**Why:** Created to ensure consistent, high-quality documentation for the design system based on Google Tech Writing course principles.

**How to apply:** Before writing documentation for any DS component, token, or API, read the relevant file(s):
- Any documentation → `tech-writing-one.md` (fundamentals) + `tech-writing-accessibility.md` (inclusive language)
- Long docs / complex structure → `tech-writing-two.md`
- Error messages / validation → `tech-writing-error-messages.md`
- Diagrams / visuals → `tech-writing-accessibility.md` (alt text section)
```

- [ ] **Step 3: Update MEMORY.md index**

Read the existing MEMORY.md and add this line under an appropriate section:

```
- [Tech Writing Reference Guides](project_writing_guides.md) — consult docs/writing-guides/ before writing DS documentation
```

- [ ] **Step 4: Commit memory files**

```bash
git -C /Users/marcinkwiatkowski/.claude/projects/-Users-marcinkwiatkowski-Desktop-Harbor-Design-System/memory add project_writing_guides.md MEMORY.md 2>/dev/null || true
```

(Memory files are not tracked by the project git repo — skip git commit for memory files.)
